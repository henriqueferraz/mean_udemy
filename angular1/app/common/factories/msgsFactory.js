(function() {
    angular.module("primeiraApp").factory("msg", [
        'toastr',
      MsgsFactory
    ]);
    function MsgsFactory(toastr) {
        function addMsg(msgs, title, method) {
          
      }
  }  
});
